export enum EnumSettingGroup {
  SYSTEM = 'system',
  APP_VERSION = 'app_version',
}

export enum EnumSettingType {
  BOOLEAN = 'boolean',
  NUMBER = 'number',
  STRING = 'string',
  ARRAY = 'array',
  JSON = 'json',
}

export enum EnumInvoiceQueue {
  SCAN_EARN_POINTS = 'invoice:scan:earn_points',
  SCAN_OVER_DUE_INVOICES = 'invoice:scan:over_due_invoices',
  PROC_EARN_POINTS = 'invoice:proc:earn_points',
  PROC_OVER_DUE_INVOICES = 'invoice:proc:over_due_invoices',
}

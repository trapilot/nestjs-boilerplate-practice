export enum ENUM_LOGGER_TYPE {
  HTTP = 'http',
  SYSTEM = 'system',
  DATABASE = 'database',
}

export enum ENUM_LOGGER_SEVERITY {
  critical = 'critical',
  error = 'error',
  warning = 'warning',
  info = 'info',
  debug = 'debug',
  trace = 'trace',
}
